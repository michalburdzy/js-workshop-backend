// HERE HANDLE ALL DATABASE LOGIC - CREATE / UPDATE / DELETE / GET / LIST

const getAllTasks = () => {
  res.json({ message: 'IMPLEMENT ME' })
}

const createTask = () => {
  res.json({ message: 'IMPLEMENT ME' })
}

module.exports = { getAllTasks, createTask }
